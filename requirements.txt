# WebAppDL/requirements.txt

# Django y WebSockets
django>=4.2,<5.0
channels>=4.0,<5.0
daphne>=4.0,<5.0
redis==5.0.1
channels-redis==4.2.0
# Opcional pero recomendado para producción:
# channels-redis>=4.1,<5.0
whitenoise==6.6.0
selenium>=4.0.0,<5.0

# Automatización Web
playwright==1.40.0 # Usamos la versión de tu script funcional

# Peticiones HTTP (NUEVO - para API de Moon)
httpx>=0.20,<0.28 # Añadido httpx

# Utilidades (algunas de scriptDL)
python-dotenv>=1.0,<2.0
jsonschema>=4.19,<5.0 # Si reutilizas la validación de schema JSON
jinja2>=3.1,<4.0     # Para reportes HTML (ya es dep de Django)

# Opcional: Para el servidor ASGI alternativo Uvicorn
# uvicorn>=0.23,<0.24
# gunicorn>=21.2,<22.0 # Si despliegas con Uvicorn+Gunicorn
